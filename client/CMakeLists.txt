project(client)
aux_source_directory(source DIR_SRCS)
include_directories( ${PROJECT_SOURCE_DIR}/include)
include_directories( ${PROJECT_SOURCE_DIR}/../common/include)
add_executable(client ${DIR_SRCS} ${NET_DIR_SRCS} ${THREADPOOL_DIR_SRCS})
